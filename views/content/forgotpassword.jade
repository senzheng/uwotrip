extends ../layouts/master

block content
    .form-horizontal
        .form-group
            .col-sm-offset-3.col-sm-6
                h3.page-header 密码忘记了？
                p 没问题 ！只要您在下面的框中输入注册帐户时使用的电子邮箱，并点击“获取密码”。我们将会给您发送关于如何重置密码的邮件。
        .form-group
            .col-md-offset-3.col-sm-6
                input#email.form-control(type='email' placeholder='邮箱 *')
        .form-group
            .col-sm-offset-3.col-sm-10
                button.btn.btn-primary#submit(type='button') 获取密码
                #message.text-danger
    script.
        $(function () {

            $('#submit').click(function () {
                $.post('/forgot', {email: $('#email').val()}, function (result) {
                    $('#message').text(result.message);
                });
            });

        });